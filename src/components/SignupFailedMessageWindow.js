import React, { useState } from 'react'
import { SafeAreaView, StyleSheet, TextInput, Button, Text, Modal, Alert } from 'react-native'

export default function SignupFailedMessageWindow(error) {
  return (
    <Text> something </Text>
  )
}
